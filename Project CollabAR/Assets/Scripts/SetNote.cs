using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetNote : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        
    }

    public void SetButton(string type)
    {
        // sets the prefab as a text prefab
        print(type);
        print("should work");

        //transform.parent.;

    }
}
